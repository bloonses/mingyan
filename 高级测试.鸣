打印("=== 高级功能测试 ===")
打印()

打印("--- 列表方法 ---")
变量 水果 = ["苹果", "香蕉", "橘子"]
打印("水果: " + 转字符串(水果))
水果.追加("葡萄")
打印("追加后: " + 转字符串(水果))
打印("包含橘子: " + 转字符串(水果.包含("橘子")))
打印("长度: " + 转字符串(水果.长度()))
打印("首个: " + 水果.首个())
打印("末个: " + 水果.末个())
打印()

打印("--- 字符串方法 ---")
变量 句子 = "  鸣言编程语言很棒  "
打印("原文: '" + 句子 + "'")
打印("去空白: '" + 句子.去空白() + "'")
打印("包含'编程': " + 转字符串(句子.包含("编程")))
打印("替换: " + 句子.替换("棒", "强"))
打印("长度: " + 转字符串(句子.长度()))
打印()

打印("--- 字典方法 ---")
变量 学生 = {"姓名": "张三", "年龄": 18, "成绩": 95}
打印("学生: " + 转字符串(学生))
打印("键列表: " + 转字符串(学生.键列表()))
打印("值列表: " + 转字符串(学生.值列表()))
打印("有键'姓名': " + 转字符串(学生.有键("姓名")))
打印("取值: " + 转字符串(学生.取值("年龄")))
打印()

打印("--- 数学函数 ---")
打印("圆周率: " + 转字符串(圆周率))
打印("自然常数: " + 转字符串(自然常数))
打印("平方根(16): " + 转字符串(平方根(16)))
打印("绝对值(-42): " + 转字符串(绝对值(-42)))
打印("上取整(3.2): " + 转字符串(上取整(3.2)))
打印("取整(3.8): " + 转字符串(取整(3.8)))
打印("四舍五入(3.5): " + 转字符串(四舍五入(3.5)))
打印()

打印("--- 类型检查 ---")
打印("是数字(42): " + 转字符串(是数字(42)))
打印("是字符串('你好'): " + 转字符串(是字符串("你好")))
打印("是列表([1,2]): " + 转字符串(是列表([1, 2])))
打印("是空(空): " + 转字符串(是空(空)))
打印("是函数(打印): " + 转字符串(是函数(打印)))
打印()

打印("--- 闭包与高阶函数 ---")
函数 创建计数器(起始) {
    变量 计数 = 起始
    函数 递增() {
        计数 += 1
        返回 计数
    }
    返回 递增
}

变量 计数器 = 创建计数器(0)
打印("计数: " + 转字符串(计数器()))
打印("计数: " + 转字符串(计数器()))
打印("计数: " + 转字符串(计数器()))
打印()

打印("=== 测试完成 ===")
